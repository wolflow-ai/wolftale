services:
  - type: web
    name: wolftale-ui
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://wolftale-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
